# 服务器故障排查报告 — 154.23.162.32（ukapi.cc）

> 排查时间：2026-09-12 23:5x（服务器本地 +08:00）
> 服务器状态：**未做任何修改**（按用户要求仅分析）

---

## 一、结论（TL;DR）

**服务器和应用程序本身没有挂，故障是 Docker 容器网络在 9月10日 服务器重启后损坏导致的。**
`deploy_sub2api-network`（172.19.0.0/16）的网桥 `br-4c9511c1ed71` 丢失了网关 IP `172.19.0.1`，
宿主机路由表缺少 `172.19.0.0/16` 路由 → 宿主机与容器完全无法互通 →
nginx 代理到 `127.0.0.1:8080`（docker-proxy → 容器）全部超时 → **网站对外表现为"挂了"（约已持续 2.5 天）**。

---

## 二、症状验证

| 检查项 | 结果 | 说明 |
|---|---|---|
| SSH (22) | ✅ 正常 | 服务器在线，可登录 |
| 系统负载 | ✅ 正常 | load 0.02；up 2 天 12 小时 |
| 内存 | ✅ 正常 | 324Mi / 1.9Gi，Swap 未用 |
| 磁盘 | ⚠️ 69% | 20G / 29G 已用，暂不紧张 |
| Nginx (80/443) | ✅ 正常 | 监听正常，80→301 跳 https |
| 容器 sub2api / postgres / redis | ✅ "Up 2 days (healthy)" | 进程都活着 |
| 容器内 localhost `/health` | ✅ `{"status":"ok"}` | docker 健康检查每 30s 通过 |
| **宿主机 → 127.0.0.1:8080** | ❌ 挂起超时 | TCP 能连上（docker-proxy），HTTP 永远无响应 |
| **宿主机 → 容器 172.19.0.3:8080** | ❌ 完全不通 | 直连也超时 |
| **容器 → 外网** | ❌ 不通 | 应用每 10 分钟拉 GitHub 价格表，全部 `dial tcp ... i/o timeout` |
| 应用日志 | ⚠️ 无 panic/死锁 | 仅后台定价同步因外网不通持续报错 |
| systemd failed | ✅ 仅 fwupd-refresh | 无害 |

---

## 三、根因分析（证据链）

### 1. 容器网络桥丢了 IP，宿主机没有对应路由

```
$ ip -br addr
br-4c9511c1ed71  UP      ← 网桥活着，但后面没有任何 IP！
br-1a872b60ad2b  DOWN    ← 另一个空网络（无容器使用）

$ ip route
default via 10.0.21.1 dev ens17      ← 没有 172.19.0.0/16 的路由！

$ docker network inspect deploy_sub2api-network
subnet=172.19.0.0/16  gw=172.19.0.1
containers: sub2api=172.19.0.3/16  sub2api-postgres=172.19.0.4/16  sub2api-redis=172.19.0.2/16

$ ip addr show br-4c9511c1ed71
<BROADCAST,MULTICAST,UP,LOWER_UP> ...  ← 只有 L2 地址，无 inet 地址
```

- 容器路由表正常（默认网关 172.19.0.1），**但 172.19.0.1 这个网关 IP 根本不存在**（网桥无 IP），
  容器 ARP 不到网关 → 容器出网全灭。
- 宿主机访问 172.19.0.3 时无路由可走 → 数据包走默认路由丢到外网 → 黑洞。
- iptables 的 DNAT/MASQUERADE 规则都还在（`127.0.0.1:8080 → 172.19.0.3:8080`），但没有路由就全是摆设。

### 2. 直接证据：nginx 报上游连接失败

```
/var/log/nginx/error.log（9/12 23:25:11）:
recv() failed (104: Unknown error) while reading response header from upstream,
client: 136.22.129.7, server: ukapi.cc, request: "GET / HTTP/2.0",
upstream: "http://127.0.0.1:8080/", host: "154.23.162.32"
```

nginx → 127.0.0.1:8080 → docker-proxy → 172.19.0.3:8080 这条路断了，正是用户看到的"网站打不开"。

### 3. 故障起点：9月10日的双重重启

```
$ journalctl --list-boots
-2  Aug 03 15:40 → Sep 10 10:27   （旧内核 5.15.0-30，稳定运行 37 天）
-1  Sep 10 10:41:08 → 10:44:26    （新内核 5.15.0-191 第一次启动，仅 3 分钟）
 0  Sep 10 10:44:39 → 现在         （当前运行的内核 5.15.0-191）
```

- `-1` 那次启动的结尾是**干净的 systemd 关机**（`Reached target System Power Off`，非内核 panic）。
- 第二次开机（10:44:39）后 dockerd 启动时报：
  - `Deleting nftables IPv4 rules error="exit status 1"`
  - `failed to determine if container is already mounted`
  - 容器 `RestartCount=1, StartedAt=2026-09-10T02:44:59Z`（= 本地 10:44:59）
- 结论：**新内核开机后 dockerd 重建桥接网络时 IP 分配失败，网桥光有 MAC 没有 IP，路由没写进主表**。之后一直维持这个坏状态，没有任何自愈。

### 4. 应用本身无辜

- `docker inspect` 健康检查连续通过（`wget localhost:8080/health` 秒回）。
- 日志 75 万行，除"外网超时"外无 panic / 死锁 / OOM。
- postgres / redis 进程正常，还有既有的空闲连接（说明断网是"新增连接不通"，存量连接仍在）。

---

## 四、次要发现（非本次故障原因，供留意）

1. **磁盘 69%**（20G/29G 已用），建议关注，避免写满。
2. nginx access log 有大量来自 `118.193.33.244` 的畸形探测流量（HTTP 400），属扫描行为，不影响运行。
3. 内存只有 1.9G，配置了 swap 2G；容器按低内存优化部署（fork 版），目前内存无压力。
4. 内核从 5.15.0-30 升级到 5.15.0-191 后第一次重启即出现网络初始化异常，建议后续观察新内核稳定性。

---

## 五、修复方案（已确认暂不执行，仅存档）

按推荐优先级：

1. **重启 Docker 服务（最干净）**
   ```bash
   systemctl restart docker
   ```
   dockerd 重启时会重建所有桥接网络并重新分配 IP/路由；3 个容器均为
   `restart=unless-stopped` 会自动拉起。中断约 30~60 秒。

2. **手工补 IP 和路由（秒级、不动容器，但为临时方案）**
   ```bash
   ip addr add 172.19.0.1/16 dev br-4c9511c1ed71
   ip link set br-4c9511c1ed71 up
   ip route add 172.19.0.0/16 dev br-4c9511c1ed71
   ```
   立即恢复网络，但下次重启可能复发，且与 docker 管理逻辑存在冲突风险。

3. **Compose 重建（按部署方式恢复）**
   ```bash
   cd /opt/sub2api/deploy
   docker compose -f docker-compose.local.yml -f docker-compose.fork.yml up -d
   ```
   会按声明重建网络（网络名 `deploy_sub2api-network` 与现有一致），可顺便核对配置漂移。

**修复后验证**：
```bash
ip route | grep 172.19            # 应有 172.19.0.0/16 dev br-4c9511c1ed71
curl -m 5 http://127.0.0.1:8080/health   # 应返回 {"status":"ok"}
curl -m 10 -I https://ukapi.cc           # 应返回 200
```

---

*报告生成：2026-09-12 23:5x (+08:00)，基于 SSH 只读诊断（uptime/free/df/docker/ss/iptables/ip route/journalctl/dmesg/nginx 日志）。未对服务器做任何写入或变更操作。*
